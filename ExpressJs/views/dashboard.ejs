<!DOCTYPE html>
<html>

<head>
    <title>Login</title>
    <style>
        :root {
            --primary: #3f87ff;
            --dark: #0b1023;
            --bg: #f6f8fc;
            --text: #333;
        }

        body {
            font-family: "Segoe UI", Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: var(--bg);
            color: var(--text);
        }

        /* NAVBAR (UNCHANGED) */
        .navbar {
            background-color: #333;
            overflow: hidden;
        }

        .navbar a {
            float: left;
            display: block;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
            font-weight: bold;
        }

        .navbar a:hover {
            background-color: #ddd;
            color: black;
        }

        /* PAGE LAYOUT */
        .login-page {
            display: grid;
            grid-template-columns: 1.1fr 1fr;
            min-height: calc(100vh - 50px);
        }

        /* BRAND SIDE */
        .brand-panel {
            background: linear-gradient(135deg, var(--primary), var(--dark));
            color: white;
            padding: 90px 70px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .brand-panel h1 {
            font-size: 44px;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .brand-panel p {
            font-size: 18px;
            max-width: 420px;
            opacity: 0.95;
            line-height: 1.6;
        }

        /* FORM SIDE */
        .form-panel {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .login-card {
            background: #fff;
            width: 100%;
            max-width: 420px;
            padding: 45px;
            border-radius: 18px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.12);
        }

        .login-card h2 {
            font-size: 26px;
            margin-bottom: 8px;
        }

        .login-card p {
            font-size: 14px;
            color: #666;
            margin-bottom: 30px;
        }

        label {
            font-size: 13px;
            font-weight: 600;
            color: #555;
            display: block;
            margin-bottom: 6px;
        }

        input {
            width: 100%;
            padding: 13px 14px;
            margin-bottom: 18px;
            border-radius: 10px;
            border: 1px solid #ccc;
            font-size: 14px;
        }

        input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(63,135,255,0.15);
        }

        .actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            font-size: 13px;
        }

        .actions a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        button {
            width: 100%;
            padding: 14px;
            border-radius: 30px;
            border: none;
            background: linear-gradient(135deg, var(--primary), #5f9cff);
            color: white;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            box-shadow: 0 12px 30px rgba(63,135,255,0.4);
            transform: translateY(-2px);
        }

        .signup {
            text-align: center;
            font-size: 14px;
            margin-top: 20px;
        }

        .signup a {
            color: var(--primary);
            font-weight: 600;
            text-decoration: none;
        }

        /* MOBILE */
        @media (max-width: 900px) {
            .login-page {
                grid-template-columns: 1fr;
            }

            .brand-panel {
                text-align: center;
                padding: 60px 30px;
            }
        }
    </style>
</head>

<body>

    <!-- NAVBAR (AS IS) -->
    <%- include('partials/navbar') %>

    <!-- LOGIN PAGE -->
    <div class="login-page">

        <!-- BRAND / INFO PANEL -->
        <div class="brand-panel">
            <h1>Sign in to your account</h1>
            <p>
                Access your dashboard, manage projects, and
                collaborate with your team securely.
            </p>
        </div>

        <!-- FORM PANEL -->
        <div class="form-panel">
            <div class="login-card">
                <h2>Thank You for login</h2>
                <p>Please Verify your account</p>
                <p>This is your Username : <%= username %></p>
                <p>This is your Password : <%= password %></p>

                <div class="signup">
                    Don’t have an account? <a href="/register">Create one</a>
                </div>
            </div>
        </div>
        
    </div>

</body>
</html>
